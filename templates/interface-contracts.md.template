<!-- Template: 生成后应编辑为实际内容 -->
# {{GROUP_NAME}} 接口契约

> 本文件记录组内项目间的接口契约。变更任何契约需经 Group-Lead 协调 + 用户审批。

## 契约登记表

| ID | 提供方 | 消费方 | 接口描述 | 状态 |
|----|--------|--------|----------|------|
| — | — | — | (待项目运行时填充) | — |

## 自动发现机制

Architect 在分析代码时会自动识别跨项目依赖并提议契约条目:
- REST API 调用 (如 auth 服务的鉴权接口被 micro 调用)
- Maven 依赖 (如 snapmaker-common 的公共类被多项目引用)
- 共享数据模型 (如跨项目的数据库表关联)

发现流程: Architect 提议 → TeamLead 转交 → Group-Lead 审核 → 更新本文件

## 契约变更流程

1. 变更发起方的 Architect 提出变更提案
2. Group-Lead 通知受影响项目的 TeamLead
3. 受影响项目的 Architect 评估兼容性
4. 所有方达成一致 → 用户审批 → 执行变更
5. 变更后更新本文件

## 契约条目模板

```
契约 ID: IC-{序号}
提供方: {项目名} / {模块名}
消费方: {项目名} / {模块名}
接口类型: REST API / Maven 依赖 / 共享库
接口描述: {简述}
当前版本: {版本号}
契约内容:
  - 端点/方法: {具体接口}
  - 请求格式: {格式描述}
  - 响应格式: {格式描述}
  - 错误码: {错误码列表}
```
